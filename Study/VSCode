VS Code
	-install emmet, its already there
	-install  	Prettier do this but I would suggest using pretty
				which automatically formats your code and then it does format each team as far as like line breaks
				and indentations stuff like that if you want your code to match mine exactly as far as spacing and so
	--live server
	--VS Code Icon
	--